import styles from "./ChatView.module.css";

const ChatView = () => {
	return (
		<div className={styles.chatView}>
			<h1>This is where the chat view is!</h1>
		</div>
	);
};

export default ChatView;
